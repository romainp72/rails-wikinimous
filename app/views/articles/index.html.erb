
<h1>Enjoy these creations below</h1>
<div>
  <ul>
    <% @articles.each do |article| %>
      <li><strong>Title : </strong> <%= link_to article.title, article_path(article) %> <br> <strong>Quote : </strong><%= article.content %>
      <br><%= link_to "🗑️", article_path(article), method: :delete %></li>
    <% end %>
  <ul>
</div>


